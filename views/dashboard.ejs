<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>MedConnect | Modern Dashboard</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Montserrat:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style>
        :root {
            --primary: #2a7cc7;
            --primary-dark: #1e5a9b;
            --secondary: #34b27b;
            --accent: #ff6b6b;
            --light: #f8f9fa;
            --dark: #1a1d21;
            --gray: #6c757d;
            --light-gray: #e9ecef;
            --card-bg: rgba(255, 255, 255, 0.85);
            --glass: rgba(255, 255, 255, 0.25);
            --glass-border: rgba(255, 255, 255, 0.18);
            --card-shadow: 0 8px 32px rgba(31, 38, 135, 0.1);
            --transition: all 0.3s ease;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Poppins', sans-serif;
            background: linear-gradient(135deg, #e0f7fa 0%, #f0f9ff 100%);
            color: var(--dark);
            min-height: 100vh;
            overflow-x: hidden;
            position: relative;
        }

        body::before {
            content: "";
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: url('data:image/svg+xml;utf8,<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 200 200"><path fill="%232a7cc710" d="M49.5,-53.4C63.8,-43.2,74.2,-25.9,75.5,-7.6C76.7,10.7,68.8,29.9,56.3,43.3C43.7,56.7,26.5,64.2,7.6,66.5C-11.3,68.8,-22.3,65.8,-32.6,58.2C-42.9,50.6,-52.5,38.4,-58.4,23.7C-64.4,9.1,-66.7,-8.1,-63.2,-23.2C-59.6,-38.3,-50.2,-51.3,-38,-62.2C-25.8,-73.1,-10.9,-81.9,4.3,-84.8C19.6,-87.7,39.2,-84.8,49.5,-53.4Z" transform="translate(100 100)" /></svg>');
            background-size: 600px;
            opacity: 0.3;
            z-index: -1;
        }

        /* Header Styles */
        .header {
            background: var(--glass);
            backdrop-filter: blur(10px);
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.05);
            padding: 15px 40px;
            position: sticky;
            top: 0;
            z-index: 100;
            border-bottom: 1px solid var(--glass-border);
        }

        .header-container {
            display: flex;
            justify-content: space-between;
            align-items: center;
            max-width: 1400px;
            margin: 0 auto;
        }

        .logo {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .logo-icon {
            font-size: 28px;
            color: var(--primary);
            background: rgba(42, 124, 199, 0.1);
            width: 45px;
            height: 45px;
            border-radius: 12px;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .logo h1 {
            font-family: 'Montserrat', sans-serif;
            font-size: 24px;
            font-weight: 700;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
            letter-spacing: -0.5px;
        }

        .user-info {
            display: flex;
            align-items: center;
            gap: 20px;
        }

        .notifications {
            position: relative;
            font-size: 20px;
            color: var(--gray);
            cursor: pointer;
        }

        .notification-badge {
            position: absolute;
            top: -5px;
            right: -5px;
            background: var(--accent);
            color: white;
            font-size: 10px;
            width: 18px;
            height: 18px;
            border-radius: 50%;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .user-avatar {
            width: 45px;
            height: 45px;
            border-radius: 50%;
            background: linear-gradient(135deg, var(--primary), var(--secondary));
            color: white;
            display: flex;
            align-items: center;
            justify-content: center;
            font-weight: 600;
            cursor: pointer;
            transition: var(--transition);
        }

        .user-avatar:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(42, 124, 199, 0.3);
        }

        /* Main Layout */
        .dashboard-container {
            display: flex;
            max-width: 1400px;
            margin: 30px auto;
            padding: 0 30px;
            gap: 30px;
        }

        /* Sidebar Styles */
        .sidebar {
            width: 250px;
            background: var(--glass);
            backdrop-filter: blur(10px);
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            padding: 25px 0;
            height: fit-content;
            border: 1px solid var(--glass-border);
        }

        .nav-item {
            padding: 15px 25px;
            display: flex;
            align-items: center;
            gap: 15px;
            color: var(--dark);
            text-decoration: none;
            transition: var(--transition);
            font-size: 16px;
            margin: 5px 10px;
            border-radius: 12px;
        }

        .nav-item:hover, .nav-item.active {
            background: rgba(42, 124, 199, 0.1);
            color: var(--primary);
        }

        .nav-item i {
            width: 24px;
            text-align: center;
            font-size: 18px;
        }

        .donate-button {
            display: block;
            margin: 20px 25px 0;
            padding: 14px;
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
            border-radius: 12px;
            text-align: center;
            font-weight: 500;
            text-decoration: none;
            transition: var(--transition);
            box-shadow: 0 5px 15px rgba(42, 124, 199, 0.3);
            border: none;
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
            letter-spacing: 0.5px;
            cursor: pointer;
        }

        .donate-button:hover {
            transform: translateY(-3px);
            box-shadow: 0 8px 20px rgba(42, 124, 199, 0.4);
        }

        .donate-button i {
            margin-right: 8px;
        }

        /* Main Content */
        .main-content {
            flex: 1;
            display: flex;
            flex-direction: column;
            gap: 30px;
        }

        .welcome-card {
            background: var(--card-bg);
            border-radius: 20px;
            min-height: fit-content;
            padding: 30px;
            box-shadow: var(--card-shadow);
            border: 1px solid var(--glass-border);
            position: relative;
            overflow: hidden;
        }

        .welcome-card::before {
            content: "";
            position: absolute;
            top: 0;
            right: 0;
            width: 40%;
            height: 100%;
            background: linear-gradient(135deg, rgba(42, 124, 199, 0.08), rgba(52, 178, 123, 0.08));
            clip-path: polygon(100% 0, 100% 100%, 0 0);
            z-index: 0;
        }

        .welcome-content {
            position: relative;
            z-index: 1;
        }

        .welcome-content h2 {
            font-family: 'Montserrat', sans-serif;
            font-size: 32px;
            margin-bottom: 10px;
            font-weight: 700;
            background: linear-gradient(to right, var(--primary), var(--dark));
            -webkit-background-clip: text;
            -webkit-text-fill-color: transparent;
        }

        .welcome-content p {
            font-size: 16px;
            color: var(--gray);
            max-width: 600px;
            line-height: 1.6;
            margin-bottom: 20px;
        }

        .welcome-stats {
            display: flex;
            gap: 20px;
            margin-top: 25px;
        }

        .stat-item {
            display: flex;
            align-items: center;
            gap: 12px;
        }

        .stat-icon {
            width: 50px;
            height: 50px;
            border-radius: 14px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 20px;
            background: rgba(42, 124, 199, 0.1);
            color: var(--primary);
        }

        .stat-text h3 {
            font-size: 24px;
            font-weight: 700;
            color: var(--dark);
        }

        .stat-text p {
            font-size: 14px;
            color: var(--gray);
            margin: 0;
        }

        /* Stats Cards */
        .stats-container {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(250px, 1fr));
            gap: 25px;
        }

        .stat-card {
            background: var(--card-bg);
            border-radius: 20px;
            padding: 25px;
            box-shadow: var(--card-shadow);
            border: 1px solid var(--glass-border);
            display: flex;
            align-items: center;
            gap: 20px;
            transition: var(--transition);
            cursor: pointer;
        }

        .stat-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 30px rgba(31, 38, 135, 0.15);
        }

        .stat-icon-card {
            width: 70px;
            height: 70px;
            border-radius: 18px;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 28px;
        }

        .stat-details {
            flex: 1;
        }

        .stat-number {
            font-size: 32px;
            font-weight: 700;
            color: var(--dark);
            font-family: 'Montserrat', sans-serif;
        }

        .stat-title {
            color: var(--gray);
            font-size: 15px;
        }

        .icon-donate {
            background: rgba(52, 178, 123, 0.12);
            color: var(--secondary);
        }

        .icon-receive {
            background: rgba(255, 107, 107, 0.12);
            color: var(--accent);
        }

        .icon-pending {
            background: rgba(255, 193, 7, 0.12);
            color: #ffc107;
        }

        .icon-completed {
            background: rgba(42, 124, 199, 0.12);
            color: var(--primary);
        }

        /* Content Sections */
        .content-section {
            background: var(--card-bg);
            border-radius: 20px;
            box-shadow: var(--card-shadow);
            padding: 30px;
            border: 1px solid var(--glass-border);
        }

        .section-header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            margin-bottom: 25px;
        }

        .section-title {
            font-family: 'Montserrat', sans-serif;
            font-size: 24px;
            font-weight: 700;
            color: var(--dark);
            position: relative;
            padding-bottom: 10px;
        }

        .section-title::after {
            content: "";
            position: absolute;
            bottom: 0;
            left: 0;
            width: 50px;
            height: 4px;
            background: linear-gradient(to right, var(--primary), var(--secondary));
            border-radius: 2px;
        }

        .view-all {
            color: var(--primary);
            text-decoration: none;
            font-weight: 500;
            display: flex;
            align-items: center;
            gap: 5px;
        }

        .view-all:hover {
            color: var(--primary-dark);
        }

        .view-all i {
            font-size: 14px;
        }

        /* Medicine Cards */
        .medicines-grid {
            display: grid;
            grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
            gap: 25px;
        }

        .medicine-card {
            background: var(--card-bg);
            border-radius: 18px;
            overflow: hidden;
            transition: var(--transition);
            box-shadow: var(--card-shadow);
            border: 1px solid var(--glass-border);
            position: relative;
        }

        .medicine-card:hover {
            transform: translateY(-8px);
            box-shadow: 0 15px 35px rgba(31, 38, 135, 0.2);
        }

        .medicine-image {
            height: 180px;
            background: linear-gradient(135deg, #e6f2ff, #f0f9ff);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary);
            position: relative;
        }

        .medicine-image i {
            font-size: 60px;
            opacity: 0.7;
        }

        .medicine-status {
            position: absolute;
            top: 15px;
            right: 15px;
            padding: 6px 15px;
            border-radius: 20px;
            font-size: 12px;
            font-weight: 600;
            letter-spacing: 0.5px;
        }

        .status-pending {
            background: rgba(255, 193, 7, 0.2);
            color: #ff9800;
        }

        .status-accepted {
            background: rgba(52, 178, 123, 0.2);
            color: var(--secondary);
        }

        .status-completed {
            background: rgba(42, 124, 199, 0.2);
            color: var(--primary);
        }

        .medicine-details {
            padding: 22px;
        }

        .medicine-name {
            font-family: 'Montserrat', sans-serif;
            font-size: 19px;
            font-weight: 700;
            margin-bottom: 12px;
            color: var(--dark);
        }

        .medicine-info {
            display: flex;
            gap: 20px;
            margin-bottom: 18px;
            font-size: 14px;
            color: var(--gray);
        }

        .medicine-quantity, .medicine-expiry {
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .card-actions {
            display: flex;
            justify-content: space-between;
            margin-top: 15px;
        }

        .action-button {
            padding: 10px 18px;
            border-radius: 10px;
            font-size: 14px;
            font-weight: 500;
            cursor: pointer;
            transition: var(--transition);
            border: none;
            font-family: 'Montserrat', sans-serif;
            font-weight: 600;
        }

        .btn-details {
            background: rgba(42, 124, 199, 0.1);
            color: var(--primary);
        }

        .btn-details:hover {
            background: rgba(42, 124, 199, 0.2);
        }

        .btn-request {
            background: linear-gradient(135deg, var(--primary), var(--primary-dark));
            color: white;
        }

        .btn-request:hover {
            background: linear-gradient(135deg, var(--primary-dark), var(--primary));
            box-shadow: 0 5px 15px rgba(42, 124, 199, 0.3);
        }

        /* Requests Table */
        .requests-table {
            width: 100%;
            border-collapse: separate;
            border-spacing: 0;
        }

        .requests-table th {
            text-align: left;
            padding: 18px 15px;
            font-weight: 600;
            color: var(--gray);
            background: rgba(42, 124, 199, 0.05);
            font-size: 14px;
            text-transform: uppercase;
            letter-spacing: 0.5px;
        }

        .requests-table th:first-child {
            border-top-left-radius: 15px;
        }

        .requests-table th:last-child {
            border-top-right-radius: 15px;
        }

        .requests-table td {
            padding: 18px 15px;
            border-bottom: 1px solid rgba(0, 0, 0, 0.05);
            font-size: 15px;
        }

        .requests-table tr:last-child td {
            border-bottom: none;
        }

        .table-row:hover {
            background: rgba(42, 124, 199, 0.03);
        }

        .medicine-cell {
            display: flex;
            align-items: center;
            gap: 15px;
        }

        .table-med-image {
            width: 50px;
            height: 50px;
            border-radius: 12px;
            background: rgba(42, 124, 199, 0.1);
            display: flex;
            align-items: center;
            justify-content: center;
            color: var(--primary);
        }

        .status-badge {
            display: inline-block;
            padding: 7px 15px;
            border-radius: 20px;
            font-size: 13px;
            font-weight: 600;
        }
        /* Add these styles to your existing CSS */

        body {
          overflow: hidden; /* Prevent entire page from scrolling */
          padding-bottom: 50px;
        }

        .dashboard-container {
          height: calc(100vh - 80px); /* Adjust based on header height */
          overflow: hidden; /* Prevent container from scrolling */
        }

        .main-content {
          flex: 1;
          display: flex;
          flex-direction: column;
          gap: 30px;
          overflow-y: auto; /* Enable scrolling only for main content */
          max-height: calc(100vh - 80px); /* Adjust based on header height */
          padding-right: 10px; /* Prevent content from touching scrollbar */
          margin-right: -10px; /* Compensate for padding */
          padding-bottom: 70px;
        }

        .main-content::-webkit-scrollbar {
          display: none;
        }

        /* Responsive */
        @media (max-width: 1100px) {
            .dashboard-container {
                flex-direction: column;
            }
            
            .sidebar {
                width: 100%;
            }
        }

        @media (max-width: 768px) {
            .header {
                padding: 15px 20px;
            }
            
            .dashboard-container {
                padding: 0 20px;
            }
            
            .stats-container {
                grid-template-columns: 1fr;
            }
            
            .medicines-grid {
                grid-template-columns: 1fr;
            }
            
            .welcome-stats {
                flex-direction: column;
                gap: 15px;
            }
        }

        /* Animation */
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }

        .stat-card, .medicine-card, .content-section {
            animation: fadeIn 0.6s ease-out;
        }
    </style>
</head>
<body>
    <!-- Header -->
    <header class="header">
        <div class="header-container">
            <div class="logo">
                <div class="logo-icon">
                    <i class="fas fa-heartbeat"></i>
                </div>
                <h1>MedConnect</h1>
            </div>
            <div class="user-info">
                <div class="notifications">
                    <i class="fas fa-bell"></i>
                    <div class="notification-badge">3</div>
                </div>
                <div class="user-avatar">JD</div>
            </div>
        </div>
    </header>

    <!-- Dashboard Container -->
    <div class="dashboard-container">
        <!-- Sidebar Navigation -->
        <aside class="sidebar">
            <a href="#" class="nav-item active">
                <i class="fas fa-home"></i>
                <span>Dashboard</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-pills"></i>
                <span>Donate Medicines</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-search"></i>
                <span>Browse Medicines</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-hand-holding-medical"></i>
                <span>My Requests</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-history"></i>
                <span>Donation History</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-user"></i>
                <span>My Profile</span>
            </a>
            <a href="#" class="nav-item">
                <i class="fas fa-cog"></i>
                <span>Settings</span>
            </a>
            <button class="donate-button">
                <i class="fas fa-plus"></i> Donate Medicine
            </button>
        </aside>

        <!-- Main Content -->
        <main class="main-content">
            <!-- Welcome Banner -->
            <div class="welcome-card">
                <div class="welcome-content">
                    <h2>Welcome back, John!</h2>
                    <p>You're making a difference by helping reduce medical waste and providing essential medicines to those in need. Keep up the great work!</p>
                </div>
            </div>

            <!-- Stats Cards -->
            <div class="stats-container">
                <div class="stat-card">
                    <div class="stat-icon-card icon-donate">
                        <i class="fas fa-pills"></i>
                    </div>
                    <div class="stat-details">
                        <div class="stat-number">24</div>
                        <div class="stat-title">Medicines Donated</div>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon-card icon-receive">
                        <i class="fas fa-hand-holding-medical"></i>
                    </div>
                    <div class="stat-details">
                        <div class="stat-number">8</div>
                        <div class="stat-title">Medicines Received</div>
                    </div>
                </div>
                <div class="stat-card">
                    <div class="stat-icon-card icon-pending">
                        <i class="fas fa-clock"></i>
                    </div>
                    <div class="stat-details">
                        <div class="stat-number">3</div>
                        <div class="stat-title">Pending Requests</div>
                    </div>
                </div>
            </div>

            <!-- Medicines Donated Section -->
            <div class="content-section">
                <div class="section-header">
                    <h3 class="section-title">Medicines You've Donated</h3>
                    <a href="#" class="view-all">View All <i class="fas fa-arrow-right"></i></a>
                </div>
                <div class="medicines-grid">
                    <!-- Medicine Card 1 -->
                    <div class="medicine-card">
                        <div class="medicine-image">
                            <i class="fas fa-capsules"></i>
                            <div class="medicine-status status-accepted">Accepted</div>
                        </div>
                        <div class="medicine-details">
                            <h4 class="medicine-name">Paracetamol 500mg</h4>
                            <div class="medicine-info">
                                <div class="medicine-quantity">
                                    <i class="fas fa-capsules"></i> 20 tablets
                                </div>
                                <div class="medicine-expiry">
                                    <i class="fas fa-calendar"></i> Dec 2024
                                </div>
                            </div>
                            <div class="card-actions">
                                <button class="action-button btn-details">Details</button>
                                <button class="action-button btn-request">Track</button>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Medicine Card 2 -->
                    <div class="medicine-card">
                        <div class="medicine-image">
                            <i class="fas fa-pills"></i>
                            <div class="medicine-status status-pending">Pending</div>
                        </div>
                        <div class="medicine-details">
                            <h4 class="medicine-name">Vitamin C</h4>
                            <div class="medicine-info">
                                <div class="medicine-quantity">
                                    <i class="fas fa-capsules"></i> 30 capsules
                                </div>
                                <div class="medicine-expiry">
                                    <i class="fas fa-calendar"></i> Mar 2025
                                </div>
                            </div>
                            <div class="card-actions">
                                <button class="action-button btn-details">Details</button>
                                <button class="action-button btn-request">Edit</button>
                            </div>
                        </div>
                    </div>
                    
                    <!-- Medicine Card 3 -->
                    <div class="medicine-card">
                        <div class="medicine-image">
                            <i class="fas fa-tablets"></i>
                            <div class="medicine-status status-completed">Completed</div>
                        </div>
                        <div class="medicine-details">
                            <h4 class="medicine-name">Amoxicillin</h4>
                            <div class="medicine-info">
                                <div class="medicine-quantity">
                                    <i class="fas fa-capsules"></i> 15 capsules
                                </div>
                                <div class="medicine-expiry">
                                    <i class="fas fa-calendar"></i> Oct 2023
                                </div>
                            </div>
                            <div class="card-actions">
                                <button class="action-button btn-details">Details</button>
                                <button class="action-button btn-request">Review</button>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Requests Section -->
            <div class="content-section">
                <div class="section-header">
                    <h3 class="section-title">Your Medicine Requests</h3>
                    <a href="#" class="view-all">View All <i class="fas fa-arrow-right"></i></a>
                </div>
                <table class="requests-table">
                    <thead>
                        <tr>
                            <th>Medicine</th>
                            <th>Quantity</th>
                            <th>Date Requested</th>
                            <th>Status</th>
                            <th>Action</th>
                        </tr>
                    </thead>
                    <tbody>
                        <tr class="table-row">
                            <td>
                                <div class="medicine-cell">
                                    <div class="table-med-image">
                                        <i class="fas fa-pills"></i>
                                    </div>
                                    <div>
                                        <div class="medicine-name">Metformin 500mg</div>
                                        <div class="medicine-expiry">Exp: Aug 2024</div>
                                    </div>
                                </div>
                            </td>
                            <td>30 tablets</td>
                            <td>Jul 25, 2023</td>
                            <td><span class="status-badge status-pending">Pending</span></td>
                            <td><button class="action-button btn-details">Details</button></td>
                        </tr>
                        <tr class="table-row">
                            <td>
                                <div class="medicine-cell">
                                    <div class="table-med-image">
                                        <i class="fas fa-capsules"></i>
                                    </div>
                                    <div>
                                        <div class="medicine-name">Lisinopril 10mg</div>
                                        <div class="medicine-expiry">Exp: Nov 2024</div>
                                    </div>
                                </div>
                            </td>
                            <td>20 tablets</td>
                            <td>Jul 18, 2023</td>
                            <td><span class="status-badge status-accepted">Accepted</span></td>
                            <td><button class="action-button btn-details">Track</button></td>
                        </tr>
                        <tr class="table-row">
                            <td>
                                <div class="medicine-cell">
                                    <div class="table-med-image">
                                        <i class="fas fa-tablets"></i>
                                    </div>
                                    <div>
                                        <div class="medicine-name">Atorvastatin 20mg</div>
                                        <div class="medicine-expiry">Exp: Feb 2025</div>
                                    </div>
                                </div>
                            </td>
                            <td>45 tablets</td>
                            <td>Jul 10, 2023</td>
                            <td><span class="status-badge status-completed">Completed</span></td>
                            <td><button class="action-button btn-details">Review</button></td>
                        </tr>
                    </tbody>
                </table>
            </div>
        </main>
    </div>

    <script>
        // Dashboard interactions
        document.addEventListener('DOMContentLoaded', function() {
            // Toggle active state for nav items
            const navItems = document.querySelectorAll('.nav-item');
            navItems.forEach(item => {
                item.addEventListener('click', function(e) {
                    e.preventDefault();
                    navItems.forEach(i => i.classList.remove('active'));
                    this.classList.add('active');
                });
            });
            
            // Card hover animations
            const cards = document.querySelectorAll('.stat-card, .medicine-card');
            cards.forEach(card => {
                card.addEventListener('mouseenter', function() {
                    this.style.transform = 'translateY(-8px)';
                });
                card.addEventListener('mouseleave', function() {
                    this.style.transform = 'translateY(0)';
                });
            });
            
            // Button interactions
            const buttons = document.querySelectorAll('.action-button, .donate-button');
            buttons.forEach(button => {
                button.addEventListener('click', function() {
                    const text = this.textContent.toLowerCase();
                    if(text.includes('donate')) {
                        alert('Opening medicine donation form...');
                    } else if(text.includes('details') || text.includes('track') || text.includes('review')) {
                        alert(`Viewing details for this ${text.includes('details') ? 'donated' : 'requested'} medicine`);
                    } else if(text.includes('edit')) {
                        alert('Edit donation details');
                    }
                });
            });
            
            // Notification bell
            const notification = document.querySelector('.notifications');
            notification.addEventListener('click', function() {
                alert('You have 3 new notifications');
            });
        });
    </script>
</body>
</html>